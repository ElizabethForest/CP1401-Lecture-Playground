# Get the first number from the user, convert it into an integer and save it to a variable called a
a = int(input("please enter a number"))

# Get the second number from the user, convert it into an integer and save it to a variable called b
b = int(input("please enter a second number"))

# Get the sum of a and b and save it into a variable called total
total = a + b

# Display the total
print("The sum of those is " + str(total))
